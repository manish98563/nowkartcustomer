providers = ["python"]

[phases.setup]
nixPkgs = ["...", "gcc"]

[phases.install]
cmds = ["pip install -r backend/requirements.production.txt"]

[start]
cmd = "cd backend && python3 -m uvicorn server:app --host 0.0.0.0 --port $PORT"
